---
title: 'send'
openapi: 'POST /emails/send'
---

# Send a transactional email using Campaign Monitor

This endpoint sends a transactional email through Campaign Monitor. The request should include the `smartEmailId`, recipient's email address (`to`), and any additional data or parameters for the email in JSON format.
